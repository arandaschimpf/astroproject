---
import PokemonCard from "../components/PokemonCard.astro";
import { getPokemonsApi } from "./api/pokemons/pokemonIndex";
type Pokemon = {
  name: string;
  url: string;
};

const PokemonArray = await getPokemonsApi();
// console.log(PokemonArray);
{
  //   PokemonArray.results.map((data: Pokemon) => {
  //     // return <PokemonCard url={data.url} />;
  //     console.log(data);
  //   });
}

const pokemonDetails: Pokemon[] = PokemonArray.results;
// console.log(pokemonDetails);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <img src="../../PokemonTitle.png" class="title" />
    {
      pokemonDetails.map((data: Pokemon) => {
        // return <PokemonCard url={data.url} />;
        // console.log(data.url);
        return <PokemonCard url={data.url} />;
      })
    }
    <!-- <PokemonCard url={PokemonArray.results[0].url} />; -->
  </body>
</html>

<style>
  body {
    /* width: 100vw; */
    display: flex;
    /* align-content: center; */
    justify-content: center;
    flex-direction: column;
  }

  .title {
    width: 80%;
    /* position: absolute; */
  }
</style>

<script>
  // import { getPokemonsApi } from "./api/pokemons/pokemonIndex";
  // const tqnto = await getPokemonsApi()
  // console.log(tqnto)
  //
</script>
